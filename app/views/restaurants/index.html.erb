<h1>Restaurants</h1>

<div class="restaurants-list">
  <ul>
    <% @restaurants.each do |restaurant| %>
     <li> <%= link_to restaurant.name, restaurant_path(restaurant.id), style:"text-decoration: none;"%> </li>
    <% end %>
  </ul>
</div>

<%= link_to "Add new restaurant", new_restaurant_path  %>
